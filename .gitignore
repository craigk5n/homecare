# Composer dependencies — installed via `composer install` from the lock
# file. Docker builds and local dev both use this workflow; there's no
# need to track vendor/ in git.
/vendor/

# Uploaded attachments (HC-130) — keep .htaccess, ignore content
data/attachments/
!data/.htaccess

# CI logs
ci-logs/

.env
coverage.xml
coverage/
coverage.txt
.phpunit.cache/
.phpunit.result.cache
var/
includes/settings.php
homecare-dump.sql
*.log
.DS_Store
COMPETITIVE_ANALYSIS.md
AGENTS.md

# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
